// /layout/Header.js
import React from 'react';
import './styles.css';

const Header = () => {
  return (
    <header className="header">
      <h1>Hotel Booking Dashboard</h1>
      {/* Add more elements like logo, navigation links, etc. */}
    </header>
  );
};

export default Header;
